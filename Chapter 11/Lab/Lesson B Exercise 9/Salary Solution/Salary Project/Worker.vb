' Name:         Worker.vb
' Programmer:   <your name> on <current date>

Option Explicit On
Option Strict On
Option Infer Off

Public Class Worker
    Private Property _Name As String
    Private Property _Salary As Double

    Public Sub New()
        _Name = ""
        _Salary = 0
    End Sub

    Public Sub New(ByVal strN As Double, ByVal dblS As Double)
        Length = dblL
        Width = dblW
    End Sub
End Class
